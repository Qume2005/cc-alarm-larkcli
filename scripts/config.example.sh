# cc-alarm-larkcli recipient config
# Copy to ~/.config/cc-alarm-larkcli/config.sh and set exactly ONE recipient.

# Send a DM to a user (open_id, ou_xxx):
RECIPIENT_USER_ID="ou_REPLACE_ME"

# OR send to a chat (chat_id, oc_xxx). Leave commented if using USER_ID above.
# RECIPIENT_CHAT_ID="oc_REPLACE_ME"

# Optional: identity. 'user' (default) or 'bot'.
# AS="user"

# Optional: min seconds between 'progress' sends. Default 300.
# THROTTLE_SECONDS=300
